<Window x:Class="doanlttq.login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:doanlttq"
        mc:Ignorable="d"
        WindowState="Maximized"
        ResizeMode="NoResize"
        Title="login" Height="450" Width="800">
    <Grid>
        <Grid >

            <Grid.Background>
                <ImageBrush ImageSource="/Background/AnhBatDau.jpg"/>
            </Grid.Background>
            <Grid.Effect>
                <BlurEffect Radius="3"/>
            </Grid.Effect>
            <Rectangle>
                <Rectangle.Fill>
                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                        <GradientStop Color="#88000000"/>
                        <GradientStop Color="#44000000" Offset="1"/>
                        <GradientStop Color="#66272727" Offset="0.153"/>
                        <GradientStop Color="#552D2D2D" Offset="0.298"/>
                    </LinearGradientBrush>
                </Rectangle.Fill>

            </Rectangle>

        </Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="0.5*"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>
            <Button  
                    Grid.Row="1"
                    Grid.Column="1"
                    Content="ORDER"
                    FontSize="50" 
                    Background="black" 
                    Foreground="Gray"
                    FontWeight="Bold"
                    BorderBrush="Gray" 
                    MouseEnter="Mouse_Enter" 
                    MouseLeave="Mouse_Leave" Click="Click_Button"
                >
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" 
                                CornerRadius="20"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="2">
                            <ContentPresenter
                             Content="{TemplateBinding Content}"
                             ContentTemplate="{TemplateBinding ContentTemplate}"
                             HorizontalAlignment="Center"
                             VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>


            </Button>
          
        </Grid>
    </Grid>
</Window>
