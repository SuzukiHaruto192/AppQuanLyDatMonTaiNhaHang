<Window x:Class="doanlttq.login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:doanlttq"
        mc:Ignorable="d"
        WindowState="Maximized"
        Title="Login" Height="450" Width="800"
        FontFamily="Segoe UI">

    <Window.Resources>
        <Style x:Key="RestaurantButtonStyle" TargetType="Button">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#FF8000" Offset="0"/>
                        <GradientStop Color="#FFB900" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="60"/>
            <Setter Property="Width" Value="250"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="Shadow" CornerRadius="30" Background="{TemplateBinding Background}" Margin="0,4,0,-4" Opacity="0.4">
                                <Border.Effect>
                                    <BlurEffect Radius="10"/>
                                </Border.Effect>
                            </Border>
                            <Border x:Name="Border" CornerRadius="30" Background="{TemplateBinding Background}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="1.05" ScaleY="1.05" CenterX="125" CenterY="30"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.95" ScaleY="0.95" CenterX="125" CenterY="30"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#FFFCEABB" Offset="0"/>
                <GradientStop Color="#FFF8B500" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Ellipse Width="500" Height="500" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,-100,-100" Opacity="0.2">
            <Ellipse.Fill>
                <SolidColorBrush Color="White"/>
            </Ellipse.Fill>
        </Ellipse>

        <Border Width="450" Height="550"
                Background="White"
                CornerRadius="40"
                VerticalAlignment="Center"
                HorizontalAlignment="Center">

            <Border.Effect>
                <DropShadowEffect BlurRadius="40" ShadowDepth="20" Direction="315" Color="Black" Opacity="0.5"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="2*"/>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="1.5*"/>
                </Grid.RowDefinitions>

                <Viewbox Grid.Row="0" Width="120" Height="120" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,10">
                    <Grid>
                        <Ellipse Fill="#FFF5E6" Width="24" Height="24"/>
                        <Path Fill="#FF7A00" Data="M11,9H9V2H7V9H5V2H3V9C3,11.12 4.66,12.84 6.75,12.97V22H9.25V12.97C11.34,12.84 13,11.12 13,9V2H11V9M16,6V14H18.5V22H21V2C18.24,2 16,4.24 16,6Z" 
              Stretch="Uniform" Margin="5"/>
                    </Grid>
                </Viewbox>

                <StackPanel Grid.Row="1" VerticalAlignment="Center">
                    <TextBlock Text="WELCOME"
                               Foreground="#FF7A00"
                               FontSize="48"
                               FontWeight="ExtraBold"
                               HorizontalAlignment="Center">
                    </TextBlock>
                    <TextBlock x:Name="Hello"
                               Text=""
                               Foreground="#FF7A00"
                               FontSize="20"
                               FontFamily="Gabriola"
                               FontWeight="ExtraBold"
                               HorizontalAlignment="Center">
                    </TextBlock>

                </StackPanel>
                <StackPanel Grid.Row="2" VerticalAlignment="Center">
                    <TextBlock Text="Số điện thoại tích điểm"
Foreground="#FF7A00"
FontSize="24"
FontWeight="ExtraBold"
HorizontalAlignment="Center">
                    </TextBlock>
                    <Border Height="35" 
             CornerRadius="20" 
             Background="White"
             BorderThickness="1.5"
             Width="260">

                        <Border.Effect>
                            <DropShadowEffect Color="Gray" Direction="270" ShadowDepth="4" BlurRadius="10" Opacity="0.2"/>
                        </Border.Effect>

                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="BorderBrush" Value="#DDDDDD"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsKeyboardFocused, ElementName=PhoneNumberTextBox}" Value="True">
                                        <Setter Property="BorderBrush" Value="#FFA500"/>
                                        <Setter Property="Effect">
                                            <Setter.Value>
                                                <DropShadowEffect Color="#FFA500" Direction="270" ShadowDepth="0" BlurRadius="8" Opacity="0.4"/>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>

                        <TextBox     x:Name="PhoneNumberTextBox"
                      Text=""
                      FontSize="20"
                      FontWeight="SemiBold"
                      Foreground="#FF7A00"
                      MaxLength="10" 
                      Background="Transparent" 
                      BorderThickness="0" 
                      VerticalContentAlignment="Center"
                      HorizontalContentAlignment="Center"
                      Padding="10,0,10,0"
                      VerticalAlignment="Stretch"
                      HorizontalAlignment="Stretch"/>

                    </Border>
                </StackPanel>
                <Button Grid.Row="3"
                        Content="START ORDER"
                        Style="{StaticResource RestaurantButtonStyle}"
                        VerticalAlignment="Top"
                        Margin="0,20,0,0"
                        Click="Click_Button"/>


            </Grid>
        </Border>
    </Grid>
</Window>