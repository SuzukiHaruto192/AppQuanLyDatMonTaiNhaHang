<Window x:Class="QuanLyBan.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QuanLyBan"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <local:StatusToColorConverter x:Key="StatusToColorConverter"/>
        <local:StatusToTextConverter x:Key="StatusToTextConverter"/>

        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#444444"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Margin="10,2">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{TemplateBinding Tag}" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" FontSize="16"/>
                                <ContentPresenter VerticalAlignment="Center" Margin="12,0,0,0"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F2F2F2"/>
                                <Setter Property="Foreground" Value="#222222"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#E0E0E0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="0,12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Border" x:Key="TableContainerStyle">
            <Setter Property="Width" Value="120"/>
            <Setter Property="Height" Value="120"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Background" Value="{Binding Status, Converter={StaticResource StatusToColorConverter}}"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect ShadowDepth="2" BlurRadius="8" Color="#AAA" Opacity="0.4"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.7"/>
                </Trigger>

                <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ContentPresenter}}" Value="True">
                    <Setter Property="BorderBrush" Value="DodgerBlue"/>
                    <Setter Property="BorderThickness" Value="3"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBlock" x:Key="TableTextStyle">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" 
                Background="#FFFFFF" 
                BorderBrush="#E0E0E0" 
                BorderThickness="0,0,1,0"
                ScrollViewer.VerticalScrollBarVisibility="Visible">
            <StackPanel Margin="10">

                <TextBlock Text="QUẢN LÝ BÀN" 
                   FontSize="18" 
                   FontWeight="SemiBold" 
                   Foreground="#007ACC"
                   Margin="15,15,0,25" 
                   HorizontalAlignment="Left"/>

                <TextBlock Text="TRẠNG THÁI" 
                   FontSize="12" 
                   FontWeight="Bold" 
                   Foreground="#AAAAAA" 
                   Margin="15,0,0,10"/>

                <StackPanel x:Name="FilterButtons">
                    <Button Style="{StaticResource MenuButtonStyle}" Tag="&#xE825;"
                            Content="Tất cả bàn"
                            FontFamily="Segoe UI"
                            Command="{Binding DSBan.FilterTableCommand}"
                            CommandParameter="Tất cả bàn">
                    </Button>
                    <Button Style="{StaticResource MenuButtonStyle}" Tag="&#xE73B;"
                            Content="Bàn trống"
                            FontFamily="Segoe UI"
                            Command="{Binding DSBan.FilterTableCommand}"
                            CommandParameter="Bàn trống">
                    </Button>
                    <Button Style="{StaticResource MenuButtonStyle}" Tag="&#xE716;"
                            Content="Đang phục vụ"
                            FontFamily="Segoe UI"
                            Command="{Binding DSBan.FilterTableCommand}"
                            CommandParameter="Đang phục vụ">
                    </Button>
                    <Button Style="{StaticResource MenuButtonStyle}" Tag="&#xE77B;"
                            Content="Đã đặt trước"
                            FontFamily="Segoe UI"
                            Command="{Binding DSBan.FilterTableCommand}"
                            CommandParameter="Đã đặt trước">
                    </Button>
                </StackPanel>

                <Separator Margin="15,25" Background="#F0F0F0"/>

                <TextBlock Text="CHI TIẾT" 
                   FontSize="12" 
                   FontWeight="Bold" 
                   Foreground="#AAAAAA" 
                   Margin="15,0,0,10"/>

                <StackPanel x:Name="InfoPlaceholder" HorizontalAlignment="Center" Margin="0,30">
                    <StackPanel.Style>
                        <Style TargetType="StackPanel">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedTable}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>
                    <TextBlock Text="&#xE82F;" FontFamily="Segoe MDL2 Assets" FontSize="40" Foreground="#E0E0E0" HorizontalAlignment="Center"/>
                    <TextBlock Text="Chọn một bàn để xem chi tiết" Foreground="#B0B0B0" Margin="0,10,0,0" HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center"/>
                </StackPanel>

                <Grid x:Name="InfoDetails">
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding DSBan.SelectedTable}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>

                    <StackPanel Margin="15,0">
                        <TextBlock FontSize="18" FontWeight="SemiBold" Foreground="#333">
                             <Run Text="Bàn số: "/>
                             <Run Text="{Binding DSBan.SelectedTable.TableNumber}" FontWeight="Bold" Foreground="#007ACC"/>
                        </TextBlock>

                        <TextBlock Margin="0,10,0,0">
                            <Run Text="Trạng thái: "/>
                            <Run Text="{Binding DSBan.SelectedTable.Status, Converter={StaticResource StatusToTextConverter}}" FontWeight="SemiBold"/>
                        </TextBlock>
                        <TextBlock Margin="0,5,0,0">
                            <Run Text="Sức chứa: "/>
                            <Run Text="{Binding DSBan.SelectedTable.Capacity}" FontWeight="SemiBold"/>
                            <Run Text=" người"/>
                        </TextBlock>

                        <Button Content="Đặt bàn" 
                                Margin="0,30,0,0" 
                                Style="{StaticResource ActionButtonStyle}" 
                                Background="#28A745"
                                Command="{Binding DSBan.BookTableCommand}"/>

                        <Button Content="Thanh toán" 
                                Margin="0,10,0,0" 
                                Style="{StaticResource ActionButtonStyle}"
                                Background="#007ACC"
                                Command="{Binding DSBan.ClearTableCommand}"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <GridSplitter Grid.Column="1" Width="5" Background="#DDD" HorizontalAlignment="Center" VerticalAlignment="Stretch"/>

        <ListBox Grid.Column="2" 
                 ItemsSource="{Binding DSBan.Tables}"
                 SelectedItem="{Binding DSBan.SelectedTable, Mode=TwoWay}" ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 BorderThickness="0" 
                 Background="Transparent">

            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Margin="10"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>

            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <ContentPresenter/>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource TableContainerStyle}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Text="{Binding TableNumber}" 
                               Foreground="White" FontSize="36" FontWeight="Bold"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>

                            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                                <TextBlock Text="&#x1F464;" FontSize="14" Foreground="White" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding Capacity}" 
                                   Foreground="White" FontSize="14" FontWeight="SemiBold" 
                                   Margin="5,0,0,0" VerticalAlignment="Center"/>
                                <TextBlock Text=" chỗ" Foreground="White" FontSize="14" Margin="2,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>


</Window>
